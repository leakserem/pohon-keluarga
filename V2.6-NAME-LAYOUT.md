# Family Tree v2.6

## Name/header correction

- Card width increased to 320px.
- Header uses a stable avatar column plus a content column.
- Inside content, name and `Keturunan` are two explicit columns.
- Long names wrap to two lines instead of collapsing to a one-character ellipsis.
- The descendant button has a fixed 96px width and cannot overlap the name.
- Layout constants are kept in sync between `config.js` and `utils/constants.js`.
